# react-skeleton

```sh
git clone git@github.com:junpei/react-skeleton.git
cd react-skeleton

git checkout-index -a -f --prefix=export.path.to/
cd export.path.to/

npm install
npm start

curl http://localhost:3000/
```
